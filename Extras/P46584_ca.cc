#include <iostream>
#include <vector>
using namespace std;

vector<double> diferencia(const vector<double>& v1, const vector<double>& v2) {
	int sizeV1 = v1.size(), sizeV2 = v2.size();
	vector<double> v3;
	if(sizeV1 > sizeV2){
		v3 = v1;
	} else {
		v3 = v2;
	}
	for(int i = 0; i < sizeV1)
	
	
	return v3;
	
}

int main(){
	return 0;
}
