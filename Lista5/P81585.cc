#include <iostream>
using namespace std;

int main(){
	int num = 0;
	while(cin >> num){
		int nTotal = 0;
		for(int i=0;i < num ; ++i){
			nTotal = nTotal*10 + num;
		}
		bool found = false;
		int sumPostN = 0;
		for(int i = num; not found and i > 0 ; ++i){
			int sumPreN = 0
			int nPre = nTotal
			for(int j = 0 ; j < i ; ++j){
				
			}
			
		}
		
		
	}
	
}
