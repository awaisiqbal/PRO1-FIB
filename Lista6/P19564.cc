#include <iostream>
#include <math.h>
using namespace std;

int valencia(int n){
	//
}


int main(){
	 int n;
	 cin >> n;
	 int vmax = valencia(n);
	 bool balanced = vmax == 0;
	 
	 while(not(equilibrat) and cin >> n){
		int v = valencia(n);
		if(v > vmax) vmax = v;
		balanced = v == 0;
	 }
	 if (balanced){
		 cout << "asd" << n << "." << endl
	 } else {
		 cout << "vamx "<< vmax << "." << endl;
	 }
	 
}
